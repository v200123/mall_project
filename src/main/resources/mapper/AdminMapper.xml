<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.fishei.competition.mapper.AdminMapper">

    <select id="update" parameterType="cn.fishei.competition.bean.Admin">
        update t_admin set password=#{newPassword} where userName=#{userName}
    </select>


</mapper>